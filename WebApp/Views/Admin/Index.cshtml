
@{
    ViewBag.Title = "Index";
}

<div class="jumbotron">
    <h1>Bienvenido @ViewBag.nombreUsu</h1>
</div>

<br>


<!--RESUMEN tramo y salida no andan ya que le paso un diccionario-->



<h4>Seleccione una operación</h4>
<div class="row" style="height:300px">
    <div class="col-md-4">
        <h3 class="btn btn-default">@Html.ActionLink("Vehiculos", "traerVehiculos")</h3>
        <h3 class="btn btn-default">@Html.ActionLink("Viajes", "traerViajes")</h3>
        <h3 class="btn btn-default">@Html.ActionLink("Paradas", "traerParadas")</h3>
        <h3 class="btn btn-default">@Html.ActionLink("Linea", "traerLinea")</h3>
        <h3 class="btn btn-default">@Html.ActionLink("Salida", "traerSalida")</h3>
        <h3 class="btn btn-default">@Html.ActionLink("Conductores", "traerConductores")</h3>
        <h3 class="btn btn-default">@Html.ActionLink("Reporte de utilidad", "SelectreporteU")</h3>
        <h3 class="btn btn-default">@Html.ActionLink("Reporte de pasajes", "selectRepP")</h3>
    </div>
    <div class="col-md-8">
    </div>
</div>

